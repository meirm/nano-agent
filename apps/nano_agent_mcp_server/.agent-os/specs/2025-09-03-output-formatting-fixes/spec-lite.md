# Output Formatting Fixes - Lite Summary

Fix output formatting issues in nano-agent CLI by adding thinking text control (--output-thinking flag), cleaning simple format output to remove unwanted conversation markers, and implementing configurable panel width for rich format. This ensures clean, parseable simple output by default while preserving debugging capability and improving rich format readability.

## Key Points
- Add --output-thinking flag to control thinking text display in simple format
- Clean simple format output by removing conversation markers and formatting artifacts
- Implement configurable panel width for rich format display to improve readability