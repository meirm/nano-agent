# Nano Cli
  * Add --dry-run, will explain its understanding of the task without actually executing it.

# Nano Agent
  * Remove session support or do not use it by default. 

# General
  * when intercepting a violation like trying to write when in read-only mode, communicate the issue to the model as the response from the tool.
  * Improve agency of models by letting them know that they should be autonomous. May be using an alternative system prompt for agentic mode.
